name: CI

on:
  pull_request:
    branches:
      - develop
      - main
      - 'feature/*'

permissions:
    contents: read
    pull-requests: write

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Run tests
        id: test
        run: |
          npm test

      - name: Parse test results
        if: failure()
        run: node scripts/parse-tests.js

      - name: Comment PR if tests failed
        if: failure()
        uses: marocchino/sticky-pull-request-comment@v2
        with:
            path: pr-comment.txt

